# RNG Command.

## `/rng`
**Parameters**

- `min?` — Smallest possible number. (Default: 0)
- `max?` — Biggest possible number. (Default: 100)

Picks a random number from `min` to `max`. (Both are inclusive)