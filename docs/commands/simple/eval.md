# Eval Command.

## `/eval`
**Parameters**

- `expression` — The math expression you want solved.

Simply just solves the math expression. Has a very limited amount of math functions, only `sin` and `cos` are available.