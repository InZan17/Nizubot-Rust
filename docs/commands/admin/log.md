# Log Command.

Logs for debugging.

## `/log get`

Gives you all log messages connected to the server / you in a log file.

## `/log add`
**Parameters**

- `message` — The message to add to the logs.

Adds a log message to the logs. You can see it when you run `/log get`.


## `/log clear`

Clears the logs for the server / user.